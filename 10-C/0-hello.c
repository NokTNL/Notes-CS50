#include <stdio.h> // Include the standard I/O library during compilation

int main(void)
{
    printf("Hello, world!\n");
}

// To compile this file:
// - Go to the directory of your file
// - Run:
//      `make 0-hello` (without .c)
//      - This uses the gcc compiler to compile it into machine code
// - File is compiled. Run:
//      `./0-hello`